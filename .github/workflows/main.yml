name: Manual Simulation Check

on:
  push:                  # 파일이 푸시될 때도 실행되고
  workflow_dispatch:     # ⭐ 이 줄이 있으면 '버튼'으로도 실행 가능!

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 코드 가져오기
        uses: actions/checkout@v3

      - name: 파일 목록 확인 (디버깅용)
        run: ls -R

      # 여기에 Icarus Verilog 설치 및 실행 코드 추가 가능
      # 예: 
      # - name: Install Icarus Verilog
      #   run: sudo apt-get install -y iverilog
